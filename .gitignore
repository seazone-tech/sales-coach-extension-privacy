# private signing key — NEVER commit
private/
*.pem

# build / deps
node_modules/
.DS_Store
Thumbs.db

# bundled installer artifacts (rebuild as needed)
*.zip
# but track the public download
!public/Seazone-Sales-Coach.zip
